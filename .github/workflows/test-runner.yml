name: Test Runner

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v4
      - name: Test du runner
        run: echo "Le runner fonctionne !"
